{
  "name": "imad-websites",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd photography && npm install && npm run build && cp -r public .next/standalone/public && cp -r .next/static .next/standalone/.next/static",
    "start": "cd photography && node .next/standalone/server.js"
  }
}
