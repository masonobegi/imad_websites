{
  "name": "imad-websites",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd photography && npm install && npm run build && cp -r photography/public photography/.next/standalone/public && cp -r photography/.next/static photography/.next/standalone/.next/static",
    "start": "cd photography && node .next/standalone/server.js"
  }
}
